MOD MERGER - BUILD INFO
Updated: 19.05.2026
Build tag: 2026-05-19zg

START THE APP:
  launcher.bat   (only .bat launcher - double-click this)
                 Requests Administrator (UAC) automatically.

Optional (no console, not admin):
  Start Mod Merger.vbs

Main script: Texturepack-Merge-GUI.ps1

Shield / style picker (app-wide):
  When a mod has 2+ variant folders, a picture dialog opens.
  Thumbnails use preview PNGs (filename does NOT start with tex).
  Only tex*.png files are converted to DDS.
  Uncheck "skip style picture picker" on the PNG tab to convert all subfolders at once.
