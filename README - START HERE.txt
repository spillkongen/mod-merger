MOD MERGER BY PHISHINABOWL
==========================

MAIN APP (Twilight Princess style + opacity + background art):
  Launch.bat
  -> Texturepack-Merge-GUI.ps1

Requires next to the script:
  Texturepack-Merge-Background.png  (restored)

PNG -> DDS uses texconv.exe from:
  DuskModConverter\texconv.exe  (already copied here)

CLI tools:
  ger.ps1
  folder organizer.ps1

Separate purple Dusk converter (optional):
  DuskModConverter\Launch.bat
